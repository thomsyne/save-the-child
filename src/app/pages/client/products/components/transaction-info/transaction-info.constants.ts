import { ModalMetaData } from "@ga/modal";
import { Validators } from "@angular/forms";


export const transactionModalData: ModalMetaData = {
  modalType: "primary",
  header: "Transaction Detail",
  buttonText: "Generate Receipt",
};
